# Porta — o Railway injeta automaticamente, nao precisa mudar
PORT=3000

# Chave JWT — TROQUE para uma string aleatoria longa em producao
JWT_SECRET=mesa-cheia-segredo-troque-em-producao

# Credenciais do usuario admin (criado automaticamente no primeiro boot)
ADMIN_EMAIL=admin@mesacheia.com
ADMIN_PASSWORD=admin123
